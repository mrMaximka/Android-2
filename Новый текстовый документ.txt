ДЗ по Lesson-5
1. Исправлены косяки (комментарии, убраны лишнии key, заменены на AREA_KEY)
2. Исправил бесконечный цикл на сервисе, теперь проверяет подписку
3. Теперь запрос погоды на любой город в RecyclerView. Есть отдельный массив с названиями городов, которые вставляются в sql запрос
4. Погоду показывает на нужном языке (русский\англ). Берет из JSON id погоды и берет описание из hashMap, который реализован в классе DescriptionMap
5. Добавил onSaveInstanceSave, где нужно
